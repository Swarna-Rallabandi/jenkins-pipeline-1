8pipeline {
    agent {
        label 'java-slave4'
    }
    tools {
        maven 'MAVEN_3.8.9' // the same name should be configured in the tools section
    }
    stages {
        stage ("Maven"){
            steps {
                echo " this is maven sectoin"
                sh "mvn --version"
            }
        }
            
    }
}
